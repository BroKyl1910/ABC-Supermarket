@model ABCSupermarketMVC.Models.Product

@{
    ViewData["Title"] = "Edit";
}

<div class="main-wrapper">
    <h1 class="title">Edit Product</h1>
    <div class="dark-hr"></div>

    <div class="card-wrapper">
        <div class="create-card">
            <div class="custom-container">
                <div class="custom-card-header">
                    <h2>Product Image</h2>
                    <div class="header-controls">
                        <input type="file" name="file" id="file" class="inputfile" />
                        <label for="file" class="upload-label"><i class="material-icons image-add-icon">add_box</i><i class="material-icons image-edit-icon">edit</i></label>
                    </div>

                </div>
                <div class="dark-hr"></div>
                <div class="image-container">
                    <label class="no-image-text">No image selected</label>
                    <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.ProductImage)" class="upload-img">
                </div>
            </div>
        </div>

        <div class="create-card details-card">
            <div class="custom-container">
                <div class="custom-card-header">
                    <h2>Product Details</h2>
                </div>
                <div class="dark-hr"></div>

                <div class="form-control-container">
                    <input type="hidden" value="@Model.ProductId" id="prod-id">

                    <input type="text" asp-for="ProductName" id="prod-name" placeholder="Product Name" class="form-input">
                    <span asp-validation-for="ProductName" class="text-danger"></span>
                    <input asp-for="ProductPrice" id="prod-price" placeholder="Product Price" class="form-input">
                    <span asp-validation-for="ProductPrice" class="text-danger"></span>
                    <textarea asp-for="ProductDesc" id="prod-desc" placeholder="Product Description" class="form-input"></textarea>
                    <span asp-validation-for="ProductDesc" class="text-danger"></span>

                </div>
                <div class="form-button-container">
                    <input id="submit-btn" type="button" class="form-button" value="Save Product">
                    <a asp-action="Index" class="back-button">Back</a>
                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

<script>
    $(".no-image-text").css("display", "none");
    $(".upload-img").css("display", "block");

    $(".image-add-icon").css("display", "none");
    $(".image-edit-icon").css("display", "block");

    //Display picture update
    $("#file").change(function (e) {
        var file = e.originalEvent.srcElement.files[0];

        var img = $(".upload-img");
        var reader = new FileReader();
        reader.onloadend = function () {
            $(img).attr("src", reader.result);
        };

        reader.readAsDataURL(file);

        $(".image-add-icon").css("display", "none");
        $(".image-edit-icon").css("display", "block");
    });

    //Submit data from file picker and form
    $('#submit-btn').click((e) => {
        var formData = new FormData();
        formData.append("ProductID", $('#prod-id').val());
        formData.append("ProductName", $('#prod-name').val());
        formData.append("ProductPrice", $('#prod-price').val());
        formData.append("ProductDesc", $('#prod-desc').val());
        formData.append("ProductImage", $('#file')[0].files[0]);

        var request = new XMLHttpRequest();
        request.open("POST", "/Products/Edit");
        request.onreadystatechange = () => {
            if (request.readyState === 4 && request.status === 200) {
                window.location.replace("/Products/Index");
            }
        }
        request.send(formData);
    });
</script>
